<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <link href = "{{ asset('bundles/gsbapplifrais/css/portal.css') }}" 
          rel = "stylesheet" type = "text/css"/>
        <title>Portal</title>
    </head>
    <body>
        <h1>Laboratoire Galaxy Swiss Bourdin</h1>
        <div class="connexion">
            {% if recapSync %}
            Synchronisation réussie. N'oubliez pas de supprimer les fiches crées sur le téléphone grâce au bouton supprimer.
            {% else %}
            erreur lors de la synchronisation.
            {% endif %}
            <a href = "{{path ('visiteur') }}"> 
            	<button> OK </button>
        	</a>
        </div>
    </body>
</html>
