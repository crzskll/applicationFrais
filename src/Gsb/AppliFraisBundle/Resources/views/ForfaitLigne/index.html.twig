{% extends '::base.html.twig' %}

{% block body -%}
<a href="{{ path('admin')}}">Retour</a>
    <h1>ForfaitLigne list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Motif</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_forfait_ligne_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.motif }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_forfait_ligne_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_forfait_ligne_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_forfait_ligne_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
