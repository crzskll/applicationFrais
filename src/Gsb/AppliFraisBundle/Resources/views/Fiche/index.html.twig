{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Fiche list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Nbjustificatifs</th>
                <th>Datemodification</th>
                <th>Montantvalide</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('gsb_fiche_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.nbJustificatifs }}</td>
                <td>{% if entity.dateModification %}{{ entity.dateModification|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.montantValide }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('gsb_fiche_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('gsb_fiche_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('gsb_fiche_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
