{% extends '::base.html.twig' %}

{% block body -%}
    <h1>HorsForfaitLigne list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Date</th>
                <th>Libelle</th>
                <th>Montant</th>
                <th>Motif</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_hors_forfait_ligne_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.libelle }}</td>
                <td>{{ entity.montant }}</td>
                <td>{{ entity.motif }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_hors_forfait_ligne_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_hors_forfait_ligne_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_hors_forfait_ligne_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
